<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prova1pdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prova1Pdm">

        <activity android:name="com.example.rec_pdm.listaVeiculos"></activity>
        <activity
            android:name="com.example.rec_pdm.Cadastro"
            android:label="@string/title_activity_cadastro"
            android:theme="@style/Theme.Prova1Pdm.NoActionBar" />
        <activity android:name="com.example.rec_pdm.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>